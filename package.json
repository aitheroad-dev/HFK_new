{
  "name": "generic-ai-crm",
  "version": "0.1.0",
  "private": true,
  "description": "Generic AI-Centric CRM Platform - Multi-tenant, White-label",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "db:generate": "turbo run db:generate --filter=@generic-ai-crm/db",
    "db:migrate": "turbo run db:migrate --filter=@generic-ai-crm/db"
  },
  "devDependencies": {
    "turbo": "^2.3.0",
    "typescript": "^5.6.3"
  },
  "packageManager": "pnpm@9.15.0",
  "engines": {
    "node": ">=20"
  }
}
